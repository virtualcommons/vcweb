version: '3'

services:
  django:
    build:
      context: ./
      args:
        RUN_SCRIPT: "./deploy/docker/dev.sh"
    image: virtualcommons/vcweb:latest
    environment:
      DJANGO_SETTINGS_MODULE: 'vcweb.settings.dev'
    ports:
      - "127.0.0.1:8000:8000"
      - "127.0.0.1:8882:8882"
