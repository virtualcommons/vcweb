language: python
sudo: false
python:
    - "3.6"
notifications:
    email: virtualcommons-dev@googlegroups.com
env: DB=postgres
before_script:
    - fab setup
install: "pip install -r requirements.txt"
script: fab coverage
after_success:
    - coveralls
