language: python
sudo: false
python:
    - "2.7"
notifications:
    email: virtualcommons-dev@googlegroups.com
env: DB=postgres
services:
    - redis
before_script:
    - fab setup
install: "pip install -r requirements.txt"
script: fab coverage
after_success:
    - coveralls
